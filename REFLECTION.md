# REFLECTION.md — AI4SE 期末项目反思报告

> 项目：AI4SE Coding Agent Harness
> 作者：AI4SE Student
> 日期：2026-07-27

---

## 一、Superpowers 技能评价

### 1.1 最有价值的技能：Brainstorming 与 Writing-Plans

在本项目中，**Brainstorming** 和 **Writing-Plans** 是最核心的两个技能，它们直接决定了项目质量的上限。

Brainstorming 的价值在于它**强化了人与机器的交互深度**。在项目启动阶段，我面对的是一个模糊的想法——"做一个编码 agent 的 harness"。但通过 Brainstorming 的多轮追问，Agent 帮我梳理出了 11 状态 FSM 的架构、反馈闭环作为重点维度的选择、护栏系统的分层设计等关键决策。在这个过程中，Agent 提出了 7 个重要修正（如 HITL 独立状态化、TOOL_ERROR 状态分离、Classifier 规则驱动等），这些修正如果我独自设计很可能会遗漏。

更重要的是，Brainstorming 的**多次交互特性**解决了一个实际问题：人在项目初期倾向于"少量多次"的信息反馈，而不是一次性给出完整规约。每次 Agent 的追问都在帮我加深对项目的理解，迫使我思考"这个决策的 trade-off 是什么"、"为什么选方案 B 而不是方案 A"。这种逐步深化的对话，比一个人闷头写文档要高效得多。

Writing-Plans 的价值在于它为后续的 Subagent 工作提供了**必要的组织框架**。没有一份颗粒度合理的 PLAN，Subagent 就像没有地图的探险者。12 个 Task 的划分、依赖关系标注、验证步骤的明确，使得后续的 TDD 实现有了清晰的执行路径。

### 1.2 其他技能在后期阶段的价值重估

在 Phase 1-15 中，TDD、Subagent-Driven Development、Code Review 等技能更像是"工程纪律的自动化执行"。但在 Phase 16-20 的大规模重构中，它们的价值被重新确认。当 Action Schema 分离和 Communication Channel 重构涉及核心模块的大幅修改时，Systematic Debugging 在跨平台编码问题（Windows GBK）中展现了结构化排查的价值，Code Review 在架构决策层面起到了把关作用。这些技能在稳定期看似可有可无，但在重构期是安全保障。

## 二、TDD 在 AI 协作下的体验

TDD（测试驱动开发）在本项目中是一个**放大器而非阻碍**。原因在于项目可以自然地分为两个阶段：

1. **核心编码阶段**（Task 1-12）：实现 Harness 内核——状态机、工具系统、护栏、反馈闭环等
2. **表现层优化阶段**（Task 13-18）：添加 CLI 交互、Renderer 抽象、可观测性等

TDD 的"先写失败测试 → 再写实现 → 再重构"流程，在第一阶段起到了**保护核心层代码**的作用。每次 Subagent 实现一个新模块时，先写测试再写代码的流程确保了：
- 模块的接口在实现之前就被明确了
- 回归测试可以捕获后续修改引入的破坏
- 测试本身就是文档，说明了模块的预期行为

特别是在第二阶段的表现层开发中，TDD 确保了核心层的 53 个测试一直保持通过，没有被误改破坏。

在 Phase 16-20 中，TDD 的价值进一步凸显。项目测试从 201 个增长到 246 个，新增的 45 个测试覆盖了跨平台兼容性（编码、路径）、UX 交互（渲染输出、分隔符）、Plan-as-Tool 集成、Communication Channel 事件驱动等新领域。测试类型的多样化反映了项目从"验证逻辑正确性"到"验证真实环境可用性"的转变。当 Action Schema 分离涉及"提取但不改变行为"的重构时，测试套件起到了安全网的作用——没有它，这种重构几乎不可能安全完成。

## 三、SPEC/PLAN 对实现质量的影响

SPEC 和 PLAN 在本项目中起到了**定位任务方向**的锚定作用。每个 Subagent 被派发时，其任务描述来源于 PLAN 中的 Task 定义，而 Task 的接口和验证步骤又来源于 SPEC 中的设计。

在多层架构中（父 Agent → Subagent），即使 Subagent 的理解出现偏差，上层还有父 Agent 的 Review 环节兜底。因此，**偏离问题的实际影响感知并不大**——Review 环节会捕获方向偏差，并指导 Subagent 修正。

这一点与我的预期不同：我原本以为 SPEC/PLAN 的模糊会频繁导致 Subagent 做错事，但实际上 Review 机制消化了大部分偏差。当然，这也说明 SPEC/PLAN 的质量仍然重要——它们决定了偏差发生的频率和 Review 的工作量。

Phase 16-20 的两个关键设计决策进一步深化了这一理解：

**Communication Channel 的重新设计**揭示了"状态与通信是正交维度"的原则。第一阶段将 respond 设计为 FSM 的一个状态节点，但在实际使用中发现：通信（LLM 输出的呈现方式）和状态机执行（下一步做什么）是两个正交维度。将它们耦合在一个状态节点中，导致了状态爆炸和维护困难。重构后，Communication Channel 独立于 FSM，通过事件（LLM_MESSAGE 事件）而非状态转移来管理通信——这本质上是"分离关注点"原则的再次验证。

**Plan-as-Tool 机制**则颠覆了"PLAN 是静态约束"的假设。原本的计划是 harness 控制的——Pre-agent 先生成计划，然后按部就班执行。但实际使用中发现，LLM 自己在执行过程中会动态调整策略。与其对抗这种灵活性，不如将"修改计划"本身作为一个工具暴露给 LLM。plan_create 和 plan_update 两个 tool 的引入，以及两级迭代计数器的设计，使得 LLM 可以在执行过程中主动修正路线。核心教训是：**Agent 系统中，流程控制权应该下放给决策者（LLM），而不是固化在基础设施中**。Harness 应该提供工具，而非强制执行工作流。

## 四、凭据、分发与跨平台：被忽视的工程要求

课程要求中的凭据安全存储和分发设计，最初我认为是"边角料"工作，但实际上它们**迫使我想清楚了很多原本会忽略的问题**：

- **架构问题 vs 逻辑问题的区分**：在接入真实 API 进行端到端验证时，暴露了 3 个 Bug。其中 Bug 3（Context 从未填充）是架构设计缺陷，而 Bug 2（状态机转移非法）是逻辑错误。这种区分不是凭据和分发本身带来的，而是在配置凭据、构建分发产物的过程中，必须把整个系统跑通一遍，自然会暴露深层问题。

- **改动的连锁反应**：凭据存储方案（`.env` + `getpass` 引导）看起来简单，但它要求：代码中不能有硬编码的 key、`.env` 必须在 `.gitignore` 中、首次运行要有引导流程。这些要求迫使我在设计初期就考虑"这个改动是否简单？是否会为后续埋雷？"

跨平台问题（Phase 17）是另一个被严重低估的领域。项目初期在 macOS/Linux 上开发，代码天然假定 UTF-8 编码和正斜杠路径。但在 Windows 环境下运行时，两个问题同时爆发：

1. **GBK 编码问题**：Windows 控制台默认使用 GBK 编码，但 LLM 返回的路径信息是 UTF-8 编码的。修复过程（Bash 重定向 + filter 扩展 + chcp 处理）表明：编码问题不是一个"加一行 .encode()"能解决的问题，它涉及整个数据管道的编码一致性。

2. **跨平台路径处理**：LLM 倾向于生成 Unix 风格的路径（正斜杠），但 Windows API 需要反斜杠或盘符。解决方案是建立 4 层模型：Prompt 层提示平台感知 → Schema 层定义抽象路径格式 → Runtime PathNormalizer 做实际转换 → JSON Recovery 层修复 LLM 输出的不合法路径。核心洞察是：**LLM 应该生成平台无关的抽象表示，由运行时负责平台适配**，而不是要求 LLM 理解所有平台的细节。

## 五、AI4SE 方法论的批判性思考

### 5.1 价值所在

AI4SE 这门课的核心价值在于回答两个问题：

1. **如何在 AI 辅助下更好地了解自己的项目？** Brainstorming 的追问机制迫使你思考那些你原本不会想到的问题（"HITL 应该是一个状态还是 Guardrail 的分支？"、"Feedback Loop 的边界在哪里？"）。这个过程本身就是一种"被迫深入理解"。

2. **如何让 AI 做出更符合自己实际需求的产出？** 这不是靠更好的提示词工程，而是靠 SPEC + PLAN + Review 的工程化流程。AI 的产出质量取决于你给它提供的约束质量。

### 5.2 核心担忧：规约是否会成为 AI 的脚镣？

随着模型能力的疯狂提升，一个值得深思的问题是：**当前这种"用详尽规约约束 AI"的方式，是否会在未来成为 AI 的脚镣？**

假设三年后，AI 的编码能力比现在提升 10 倍，一个模糊的描述就能生成高质量代码——那么我们现在投入大量精力编写的 SPEC 和 PLAN，会不会反而限制了 AI 的发挥空间？就像给一个奥运短跑选手画了一条 10 米的跑道——他本可以跑得更远。

当然，现在的现实是：AI 仍然需要明确的约束来保证产出质量。但这个矛盾会随着模型能力的提升而加剧。

### 5.3 正交性设计：从 Communication Channel 学到的教训

Communication Channel 的重构是项目后期最有价值的架构教训。初始设计中，respond 被建模为 FSM 的一个状态——Agent 执行完毕后进入 RESPOND 状态，在那里组织输出并等待下一轮输入。这个设计看似自然，但实际问题在于：**通信方式与执行状态是两个独立的维度**。

Agent 可以在 RUNNING 状态下输出中间结果（tool output preview），也可以在 PAUSED 状态下输出提示信息，还可以在 FINISHED 状态下输出最终结果。将"输出"绑定在 RESPOND 状态上，意味着所有其他状态的输出逻辑都被扭曲了——必须在其他状态中"临时进入输出模式"。重构后将 Communication Channel 设计为独立的事件驱动层，任何状态都可以通过 LLM_MESSAGE 事件发布消息。核心原则：**正交关注点不应该被耦合在同一个状态机节点中**。如果早期就意识到这一点，Phase 16 的大规模重构可以避免。

### 5.4 真实用户测试的不可替代性

Phase 18-20 的 UX 改进全部来自真实使用体验，没有一项是在设计阶段预见到的：

- **Banner 重新设计**：用户反馈"不知道系统在做什么"，导致对横幅信息重新组织和分级
- **Tool Output Preview**：Agent 执行工具时用户看到的是"黑盒"，不知道工具是否在正常运行
- **停止分隔符**：Agent 输出和系统提示混在一起，用户分不清信息来源
- **Ctrl+C 保持对话**：用户中断执行后不想退出系统，而是想修正输入后重试
- **OS 提示和 GBK 修复**：来自 Windows 用户的真实反馈

这些改进验证了一个朴素的工程原则：**再好的架构设计，也无法替代真实用户的使用反馈**。UX 设计的迭代必须基于实际观察，而不是基于假设。

### 5.5 人的上限依然重要

无论 AI 如何发展，**人的上限（human ceiling）仍然是项目质量的最终决定因素**。具体体现在：
- 辨别 AI 输出质量的能力
- 发现架构层面缺陷的洞察力
- 在关键决策点上做出正确选择的能力

在未来，**人与人的沟通**（需求传递、团队协作）和**人与 AI 的沟通**（约束设计、结果审查）会成为核心能力。Superpowers 框架在这方面提供了有价值的训练——它不是在教你怎么用 AI，而是在教你怎么在 AI 时代做工程。

## 六、如果重做，我会改变什么

如果重新开始这个项目，我会在以下方面做出调整：

1. **更早进行真实 API 验证**：Mock 模式下的测试通过后，应该更早地用真实 API 跑一遍端到端流程。3 个 Bug 暴露得越晚，修复成本越高。

2. **减少 Serial Review 的等待**：当前 12 个 Task 按顺序串行执行，虽然有简化 Review 的好处，但效率较低。如果 Task 之间没有依赖关系，应该真正并行化。

3. **更关注 Context Engineering 层**：Bug 3 暴露的 Context Engineering 缺失是项目中最有价值的发现。如果在设计阶段就意识到"状态数据和模型输入之间的桥梁"是独立职责，早期的架构会更干净。

4. **Communication Channel 从 Phase 1 就独立设计**：将通信与状态机解耦的教训来得太晚。如果从一开始就将 Communication Channel 设计为事件驱动层而非 FSM 状态，可以避免 Phase 16 的大规模重构。

5. **跨平台测试从立项就纳入考虑**：如果开发初期就在 Windows 环境下运行测试，GBK 编码和路径问题可以更早暴露，而不是等到 Phase 17 才集中修复。

6. **UX 迭代从 Phase 1 就引入手动测试**：UX 改进虽然技术含量不高，但它们决定了用户对系统的第一印象。更早引入用户测试（哪怕是手动测试），这些改进可以更早落地。

## 七、总结

本项目通过 Superpowers 框架完成了一个完整的 Coding Agent Harness 开发周期。最大的收获不是代码本身，而是对"AI 时代工程方法论"的批判性理解：

- Brainstorming 和 Writing-Plans 是当前最有价值的技能，因为它们解决了"人如何与 AI 高效协作"的核心问题
- TDD 在 AI 协作下是有效的保护机制，特别是在分层架构中保护核心层不被误改
- 凭据和分发这类"非功能性需求"在实践中迫使开发者思考更深层的工程问题
- Context Engineering 层是"状态数据 → 模型输入"的关键桥梁，早设计比晚修复成本低得多
- 随着 AI 能力的提升，"规约是否成为脚镣"的矛盾会越来越突出，这需要工程方法论的持续演进

Phase 16-20 的工作进一步丰富了这些认知：

- **正交性设计**是系统架构中最重要的原则之一。Communication Channel 重构证明，将正交维度解耦到独立层中，比"在一个维度内模拟另一个维度"要清洁得多
- **真实环境的复杂性永远超出设计预期**。跨平台问题和 UX 反馈表明，测试通过不等于系统可用。GBK 编码、路径分隔符、控制台行为——这些"基础设施细节"最终决定了项目能否在目标环境中正常运行
- **LLM 应该控制流程，基础设施应该提供服务**。Plan-as-Tool 的核心理念是提供工具而非强加流程——这一原则可能适用于更广泛的 Agent 系统设计

最终，这个项目的核心启示是：**AI 不会降低对人的要求，它会改变对人的要求**——从"写代码"的能力，转向"设计约束、判断质量、做出决策"的能力。

---

### 最终项目数据

- 测试总数：246 个（从 Phase 15 的 201 个增长）
- 核心状态机：15 个状态 + 11 个 Guard Condition
- Communication Channel：事件驱动，独立于 FSM
- 跨平台支持：Windows + Linux/macOS（4 层路径模型）
- 工具系统：15 个工具 + 2 个 Plan 控制工具 + CONTROL_SCHEMAS 独立模块
- 交互模式：实时命令预览、输入轮询（msvcrt.kbhit）、中断保持
